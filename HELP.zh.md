# 使用说明 —— 如何运行和编辑你的网站

[English](HELP.md) · **中文**

这是一份大白话指南，全程不需要写任何代码。

- [从这里开始](#从这里开始)
- [完整流程](#完整流程)
- [每个标签页的作用](#每个标签页的作用)
- [写一篇文章](#写一篇文章)
- [插入图片](#插入图片)
- [调整外观](#调整外观)
- [发布](#发布)
- [文件都放在哪里](#文件都放在哪里)
- [出问题时怎么办](#出问题时怎么办)
- [不装任何东西也能改](#不装任何东西也能改)

---

## 从这里开始

打开「终端」（Terminal），进入项目文件夹，运行：

```bash
npm install    # 只有第一次，或者拉取了新代码之后需要
npm run edit
```

`npm run edit` 会一次启动所有服务，并自动打开浏览器：

| | 地址 | 用途 |
| --- | --- | --- |
| **工作台（Studio）** | http://127.0.0.1:4321 | 在这里编辑 |
| **预览（Preview）** | http://localhost:3000 | 你的真实网站，边改边更新 |

改完后，在那个终端窗口里按一次 **Ctrl-C**，两个服务会一起关闭。

> 工作台只在你自己的电脑上运行，别人访问不到，它也不会被发布到网站上。

---

## 完整流程

1. `npm run edit`
2. 在工作台里随便改
3. 在预览里确认效果
4. 点击 **Publish（发布）**
5. 等大约一分钟 —— 改动就出现在 https://rwteefz.github.io/ 上了

你输入的内容会随时保存到本地电脑。**不点发布，任何东西都不会上传到网上。**

---

## 每个标签页的作用

**Look（外观）** —— 主题、明暗模式、字体、标题粗细、文字大小、Logo，以及每页显示多少篇文章。

**Profile（个人信息）** —— 姓名、一句话签名、身份、所在地、链接、搜索引擎看到的标题、页脚，以及联系邮箱。

**Sections（板块）** —— 用 ↑ ↓ 调整板块顺序，用 Visible/Hidden 显示或隐藏整个板块，修改板块标题，并编辑 Now、Projects、Education、Activities 的内容。

**Articles（文章）** —— 新建、重命名、删除文章，带实时预览。

**Pictures（图片）** —— 上传图片、复制图片地址、删除图片。

**Publish（发布）** —— 把改动发送到 GitHub。

---

## 写一篇文章

1. 进入 **Articles** → 点 **+ New article**
2. 输入标题，点确定
3. 填好上面的输入框，然后在下面的大框里写正文

**Web address（网址）** 决定文章链接。网址填 `my-first-post` 的文章，地址就是
`/writing/my-first-post`。只能用小写字母、数字和连字符，**不能有空格和中文**。

**Summary（摘要）** 是首页上显示在标题下面的那一行字。

### 排版写法

正文用的是 Markdown，其实就是普通文字加几个记号：

| 你输入 | 显示效果 |
| --- | --- |
| `## 小节标题` | 标题 |
| `### 更小的标题` | 更小的标题 |
| `**重点**` | **重点** |
| `*强调*` | *强调* |
| `[显示的文字](https://example.com)` | 链接 |
| `- 一条` | 圆点列表 |
| `1. 第一步` | 数字列表 |
| `> 一段引用` | 缩进的引用 |
| `` `一段代码` `` | 等宽字体的代码 |

空一行就是新的一段。下面的 **Preview（预览）** 会实时显示最终效果。

### 草稿

网址以 `_` 开头的文章是草稿：只存在你电脑上，永远不会出现在网站上。
`_template.md` 就是这样一份模板，可以照着抄，但不要删。

---

## 插入图片

**在文章里插图：** 把光标放到想插入的位置，点 **Add a picture**，选一张图。系统会自动上传并插入好链接。

**文章顶部的大图：** 先在 **Pictures** 里上传，点 **Copy address** 复制地址，粘贴到文章的 **Cover picture** 框里。

**站点 Logo：** 同理 —— 上传、复制地址，粘贴到 **Look → Logo**。留空则显示你的名字文字。

有两个好习惯：

- **写好图片描述。** `![描述](/images/photo.jpg)` 里的描述会念给使用读屏软件的人听，图片加载失败时也会显示出来。
- **先把大图压小**，宽度 1600 像素左右就够了。手机原图常常有 5–10 MB，会让网站变慢、仓库变大。

---

## 调整外观

**Look → Theme** 里有四套完整设计，点一下就切换：

| 主题 | 风格 |
| --- | --- |
| **Ink** | 暖白纸感、衬线标题、墨绿色。安静、像书刊。 |
| **Slate** | 冷白底、紧凑无衬线标题、电光蓝。利落、现代。 |
| **Archive** | 羊皮纸配暗红、高对比衬线。像学术期刊。 |
| **Noir** | 深石墨色、等宽标题、青绿点缀。克制的终端风。 |

下面还可以在主题基础上继续微调：

- **Starting colour mode（初始明暗）** —— 亮色、暗色，或跟随访客设备。无论选哪个，访客都能用 ☾/☀ 按钮自己切换，并且会被记住。
- **Heading / Body font（标题、正文字体）** —— 衬线、无衬线、等宽。
- **Heading weight（标题粗细）** —— 从 Light（细）到 Extra bold（特粗）。
- **Text size（文字大小）** —— 整站文字一起缩放。
- **Accent colour（强调色）** —— 链接和高亮用的颜色。留空则用主题自带的。

一边改一边看预览。如果觉得不好看，把这些框恢复成默认选项即可。

---

## 发布

进入 **Publish**，写一句话说明你改了什么，然后点 **Check and publish（检查并发布）**。

它会依次做三件事：

1. 构建网站并运行检查
2. 把改动记入项目历史
3. 发送到 GitHub

之后 GitHub 会自己重新构建并上线，大约需要一分钟。刷新
https://rwteefz.github.io/ 就能看到。

如果检查发现问题，**不会发布任何东西**，并会告诉你哪里坏了。这正是它的价值：
挡住有问题的网站上线。修好后再试一次；如果你确定没问题，也可以用
**Publish without checking（跳过检查直接发布）**。

---

## 文件都放在哪里

网站上的一切都来自三个地方：

| 文件或文件夹 | 存放内容 |
| --- | --- |
| `content/site.json` | 个人信息、板块、主题，以及所有短文字 |
| `content/posts/` | 每篇文章一个文件 |
| `public/images/` | 所有图片 |

工作台会帮你改这三处。你也可以用任何文本编辑器直接打开它们。

---

## 出问题时怎么办

**`npm run edit` 提示找不到命令。**
说明没装 Node.js，或者版本太老，需要 22 或更新的版本。用 `node --version` 查看。

**提示 "The studio did not start on port 4321."**
4321 端口被别的程序占用了。关掉那个程序，或者换一个端口启动：

```bash
PORT=4400 npm run studio
```

**localhost:3000 打不开。**
刚启动时它要先构建，等几秒。还不行就按 Ctrl-C 停掉，重新运行 `npm run edit`。

**图片不显示。**
检查地址是不是以 `/images/` 开头，文件名是否完全一致（**大小写也要一样**）。
不确定就到 **Pictures** 里重新上传一次。

**发布失败。**
看黑框里的提示。常见原因有两个：没有网络；或者你上次拉取之后，GitHub 上的项目
被改过了。第二种情况在终端里运行 `git pull`，然后重新发布。

**发布完网站没变。**
先等满一分钟再刷新。如果还是旧的，多半是浏览器缓存 —— 按住 Shift 再刷新。

**我改乱了，想撤销。**
每一次发布都记录在项目历史里，东西不会真的丢失。**不要自己删文件**，找人帮忙恢复。

---

## 不装任何东西也能改

你也可以完全在浏览器里、在 github.com 上修改：

- **改文字** —— 打开 `content/site.json`，点铅笔图标，修改引号之间的文字，然后点
  **Commit changes**。引号、逗号、括号都要原样保留。
- **写新文章** —— 打开 `content/posts/`，选 **Add file → Create new file**，
  命名为 `my-article.md`，照着 `_template.md` 的格式写。
- **传图片** —— 打开 `public/images/`，选 **Add file → Upload files**，然后在文章里用
  `![描述](/images/你的文件名.jpg)` 引用。

GitHub 会自动重新构建并上线，和点发布按钮的效果一样。
